apiVersion: v1
kind: Pod
metadata:
  namespace: test
  name: task-f
  labels:
    app: task-f
spec:
  containers:
  - image: ubuntu
    name: ubuntu
    tty: true
    stdin: true
    resources:
      limits:
        cpu: "200m"
        memory: "1Gi"

